const colors = require('colors');

function generate(bananaNbr) {
    for (let bananaCount = 0; bananaCount < bananaNbr; bananaCount++) {
        console.log("banane".random);
    }
}

module.exports = generate;